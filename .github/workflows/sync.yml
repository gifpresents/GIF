name: Automated Video Library Sync

on:
  schedule:
    # Runs automatically every day at midnight UTC
    - cron: '0 0 * * *'
  workflow_dispatch:
    # Allows you to click a button to force-sync manually at any time

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository Code
      uses: actions/checkout@v4

    - name: Setup Node.js Environment
      uses: actions/setup-node@v4
      with:
        node-node: '20'

    - name: Install Runtime Dependencies
      run: npm install axios

    - name: Execute Safe Data Sync Script
      env:
        YOUTUBE_KEY: ${{ secrets.YOUTUBE_KEY }}
      run: node sync.js

    - name: Commit and Push Updated data.json
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "actions@github.com"
        git add data.json
        git diff --quiet && git diff --cached --quiet || (git commit -m "Automated Video DB Update [skip ci]" && git push)
